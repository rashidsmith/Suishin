Fix Step Progression (Replace Current Steps)
The steps are still showing the old progression. Update to the correct workflow.
Current (Wrong): Persona → Topic → Delivery Mode → AI Generation → 4C → Review
Should Be: Persona → Topic → Generate IBOs → Choose Modality → Build 4C → Review

Update the step configuration in Session Builder:

javascript// Replace the existing step configuration with:
const SESSION_STEPS = [
  { 
    id: 'persona', 
    title: 'Select Persona', 
    icon: '👥',
    description: 'Choose your target learner'
  },
  { 
    id: 'topic', 
    title: 'Topic & Goals', 
    icon: '🎯',
    description: 'Define learning focus'
  },
  { 
    id: 'generate-ibos', 
    title: 'Generate IBOs', 
    icon: '🤖',
    description: 'AI creates learning objectives'
  },
  { 
    id: 'choose-modality', 
    title: 'Choose Modality', 
    icon: '🚀',
    description: 'Select delivery method'
  },
  { 
    id: 'build-4c', 
    title: 'Build 4C Map', 
    icon: '📚',
    description: 'AI creates activity structure'
  },
  { 
    id: 'review', 
    title: 'Review & Create', 
    icon: '✅',
    description: 'Finalize session'
  }
];

Update step routing logic:

javascriptconst renderStepContent = (currentStep) => {
  switch (currentStep) {
    case 'persona':
      return <PersonaSelectionStep onNext={() => goToStep('topic')} />;
    case 'topic':
      return <TopicAndGoalsStep onNext={() => goToStep('generate-ibos')} />;
    case 'generate-ibos':
      return <GenerateIBOsStep onNext={() => goToStep('choose-modality')} />;
    case 'choose-modality':
      return <ModalitySelectionStep onNext={() => goToStep('build-4c')} />;
    case 'build-4c':
      return <Build4CStep onNext={() => goToStep('review')} />;
    case 'review':
      return <ReviewAndCreateStep />;
    default:
      return <PersonaSelectionStep />;
  }
};

Remove the "Continue to Activities" button from IBO step:


Replace with "Continue to Modality Selection"
Ensure it goes to modality step, not 4C step

Test: Step progression shows 6 steps with correct names and flow.